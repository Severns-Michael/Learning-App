// Preload script. Intentionally empty for M1 — the renderer talks to Django
// directly over HTTP. Add contextBridge APIs here if/when the renderer needs
// to invoke Node/Electron-only capabilities.
export {};
